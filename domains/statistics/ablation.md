# Ablation Studies

- Remove ONE component at a time (not groups)
- Keep all other factors frozen (seed, data, compute)
- Include a "full method" row + a "baseline" row
- Report the DELTA, not just absolute numbers
- Ablate in the order that matches your contribution list
