# TikZ Tips

- Use `\tikzset` to define reusable styles
- `pgfplots` for 2D/3D data plots; `pgfplotstable` for CSV
- Compile with lualatex for large figures (memory)
- Externalize: `\tikzexternalize` to cache — speeds up iteration
