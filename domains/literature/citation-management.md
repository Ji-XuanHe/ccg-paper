# Citation Management

- Use BibTeX exported from venue (arxiv abs page → bibtex)
- Keys: `author-year-keyword` (e.g. `vaswani-2017-attention`)
- De-dup via `bibtool` or manual pass
- Prefer published venue over arxiv when both exist
- Include DOI / URL for web-only
