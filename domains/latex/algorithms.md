# LaTeX Algorithms

- `algorithm` + `algpseudocode` (or `algorithm2e`).
- Caption ABOVE (like table).
- Comments: `\Comment{...}` right-aligned.
- Indent nested blocks. Use `\State`, `\For`, `\If`, `\Return`.
